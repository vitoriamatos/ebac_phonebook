class Contact {
  id: number
  name: string
  number: string
  group: string

  constructor(id: number, name: string, number: string, group: string) {
    this.id = id
    this.name = name
    this.number = number
    this.group = group
  }
}

export default Contact
